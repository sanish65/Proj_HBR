export enum LeadStatus {
  NEW = 'New',
  CONTACTED = 'Contacted',
  QUALIFIED = 'Qualified',
  LOST = 'Lost',
}

export enum Source {
  WEB = 'Web',
  REFERRAL = 'Referral',
  PARTNER = 'Partner',
}
